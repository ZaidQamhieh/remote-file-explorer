<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network access to the host agent (LAN / Tailscale over HTTPS). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Camera for scanning the pairing QR code. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- In-app updater: hand the downloaded APK to Android's package installer
         via our own FileProvider + ACTION_VIEW (see MainActivity.installApk).
         The system installer manages the "install unknown apps" permission
         natively, which is reliable on Android 16 (unlike a Dart-side check). -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>
    <!-- Keep transfers running while the app is backgrounded, via a foreground
         service, and show their progress/result notifications. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Photo backup: read the device's images (DCIM). READ_MEDIA_IMAGES on
         Android 13+; READ_EXTERNAL_STORAGE (capped at API 32) on older. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <!-- Biometric app lock (local_auth). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <application
        android:label="Remote File Explorer"
        android:name="${applicationName}"
        android:allowBackup="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- "Share to Remote File Explorer" — single file from any app
                 (gallery, files, browser…), handled by receive_sharing_intent. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>
            </intent-filter>
            <!-- "Share to Remote File Explorer" — multiple files at once. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>
            </intent-filter>
            <!-- Tasker / automation hook: jump straight to a paired host's
                 explorer screen. Addressed by explicit package+class+action
                 (e.g. Tasker's "Send Intent" action), not by data matching,
                 so no <data> element is needed. -->
            <intent-filter>
                <action android:name="com.zqamhieh.remote_file_explorer.OPEN_HOST"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
        <!-- FileProvider used by the in-app updater to grant the system
             installer read access to the downloaded APK. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>
        <!-- Foreground service that keeps file transfers alive while the app is
             backgrounded (see TransferService.kt). -->
        <service
            android:name=".TransferService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- Skia fallback: Impeller 3.29 glyph-atlas corruption on Samsung Mali/Xclipse GPUs -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
